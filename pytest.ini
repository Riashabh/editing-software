[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra -q
markers =
    needs_libass: requires an FFmpeg build with the libass (ass/subtitles) filters
    needs_ffmpeg: requires ffmpeg/ffprobe on PATH
